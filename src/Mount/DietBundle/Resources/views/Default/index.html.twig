{% extends 'MountDietBundle::layout.html.twig' %}

{% block content %}

  <h2 class="well">{{ day.date.format('l d.m.Y') }}</h2>

  <ul class="list-group">
    <li class="list-group-item">Kalorie <span class="badge">{{ (day.calories) ? day.calories : 0 }}</span></li>
    <li class="list-group-item">Białka <span class="badge">{{ day.protein ? day.protein : 0 }}</span></li>
    <li class="list-group-item">Węglowodany <span class="badge">{{ day.carbs ? day.carbs : 0 }}</span></li>
    <li class="list-group-item">Tłuszcze <span class="badge">{{ day.fat ? day.fat : 0 }}</span></li>
  </ul>

  <div class="panel-group" id="accordion">

    <div class="panel panel-default">
      <div class="panel-heading">
        <h4 class="panel-title">
          <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion" href="#collapseBreakfast">Śniadanie</a>
        </h4>
      </div>
      <div id="collapseBreakfast" class="panel-collapse collapse">
        <div class="panel-body">
          {% if day.getBreakfast().count() %}
            {% for breakfast in day.getBreakfast() %}
                <h3>{{ breakfast.getName() }}</h3>
                <div class="day-meal-header">
                    Kalorie: {{ breakfast.getCalories() }}
                    Białka: {{ breakfast.getProtein() }}
                    Węglowodany: {{ breakfast.getCarbs() }}
                    Tłuszcze: {{ breakfast.getFat() }}
                </div>

                <table>
                    <tr>
                        <th>Nazwa</th>
                        <th>Waga</th>
                        <th>Kalorie</th>
                        <th>Białka</th>
                        <th>Węglowodany</th>
                        <th>Tłuszcze</th>
                    <tr>

                    {% for food in breakfast.getFood() %}
                    <tr class="meal-food">
                        <td>{{ food.getFood.name }}</td>
                        <td>{{ food.serving }}</td>
                        <td>{{ food.calories }}</td>
                        <td>{{ food.protein }}</td>
                        <td>{{ food.carbs }}</td>
                        <td>{{ food.fat }}</td>
                    </tr>
                    {% endfor %}
                </table>
            {% endfor %}
          {% else %}
              <a class="btn btn-primary btn-block" href="{{ path('mount_diet_days_day', {'date': "now"|date("Ymd")}) }}">Dodaj posiłki</a>
          {% endif %}
        </div>
      </div>
    </div>

    <div class="panel panel-default">
      <div class="panel-heading">
        <h4 class="panel-title">
          <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion" href="#collapseSecondBreakfast">Drugie Śniadanie</a>
        </h4>
      </div>
      <div id="collapseSecondBreakfast" class="panel-collapse collapse">
        <div class="panel-body">
          {% if day.getSecondBreakfast().count() %}
            {% for secondBreakfast in day.getSecondBreakfast() %}
                <h3>{{ secondBreakfast.getName() }}</h3>
                <div class="day-meal-header">
                    Kalorie: {{ secondBreakfast.getCalories() }}
                    Białka: {{ secondBreakfast.getProtein() }}
                    Węglowodany: {{ secondBreakfast.getCarbs() }}
                    Tłuszcze: {{ secondBreakfast.getFat() }}
                </div>

                <table>
                    <tr>
                        <th>Nazwa</th>
                        <th>Waga</th>
                        <th>Kalorie</th>
                        <th>Białka</th>
                        <th>Węglowodany</th>
                        <th>Tłuszcze</th>
                    <tr>

                    {% for food in secondBreakfast.getFood() %}
                    <tr class="meal-food">
                        <td>{{ food.getFood.name }}</td>
                        <td>{{ food.serving }}</td>
                        <td>{{ food.calories }}</td>
                        <td>{{ food.protein }}</td>
                        <td>{{ food.carbs }}</td>
                        <td>{{ food.fat }}</td>
                    </tr>
                    {% endfor %}
                </table>
            {% endfor %}
          {% else %}
              <a class="btn btn-primary btn-block" href="{{ path('mount_diet_days_day', {'date': "now"|date("Ymd")}) }}">Dodaj posiłki</a>
          {% endif %}
        </div>
      </div>
    </div>

    <div class="panel panel-default">
      <div class="panel-heading">
        <h4 class="panel-title">
          <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion" href="#collapseDinner">Obiad</a>
        </h4>
      </div>
      <div id="collapseDinner" class="panel-collapse collapse">
        <div class="panel-body">
          {% if day.getDinner().count() %}
            {% for dinner in day.getDinner() %}
                <h3>{{ dinner.getName() }}</h3>
                <div class="day-meal-header">
                    Kalorie: {{ dinner.getCalories() }}
                    Białka: {{ dinner.getProtein() }}
                    Węglowodany: {{ dinner.getCarbs() }}
                    Tłuszcze: {{ dinner.getFat() }}
                </div>

                <table>
                    <tr>
                        <th>Nazwa</th>
                        <th>Waga</th>
                        <th>Kalorie</th>
                        <th>Białka</th>
                        <th>Węglowodany</th>
                        <th>Tłuszcze</th>
                    <tr>

                    {% for food in dinner.getFood() %}
                    <tr class="meal-food">
                        <td>{{ food.getFood.name }}</td>
                        <td>{{ food.serving }}</td>
                        <td>{{ food.calories }}</td>
                        <td>{{ food.protein }}</td>
                        <td>{{ food.carbs }}</td>
                        <td>{{ food.fat }}</td>
                    </tr>
                    {% endfor %}
                </table>
            {% endfor %}
          {% else %}
              <a class="btn btn-primary btn-block" href="{{ path('mount_diet_days_day', {'date': "now"|date("Ymd")}) }}">Dodaj posiłki</a>
          {% endif %}
        </div>
      </div>
    </div>

    <div class="panel panel-default">
      <div class="panel-heading">
        <h4 class="panel-title">
          <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion" href="#collapseTea">Podwieczorek</a>
        </h4>
      </div>
      <div id="collapseTea" class="panel-collapse collapse">
        <div class="panel-body">
          {% if day.getTea().count() %}
            {% for tea in day.getTea() %}
                <h3>{{ tea.getName() }}</h3>
                <div class="day-meal-header">
                    Kalorie: {{ tea.getCalories() }}
                    Białka: {{ tea.getProtein() }}
                    Węglowodany: {{ tea.getCarbs() }}
                    Tłuszcze: {{ tea.getFat() }}
                </div>

                <table>
                    <tr>
                        <th>Nazwa</th>
                        <th>Waga</th>
                        <th>Kalorie</th>
                        <th>Białka</th>
                        <th>Węglowodany</th>
                        <th>Tłuszcze</th>
                    <tr>

                    {% for food in tea.getFood() %}
                    <tr class="meal-food">
                        <td>{{ food.getFood.name }}</td>
                        <td>{{ food.serving }}</td>
                        <td>{{ food.calories }}</td>
                        <td>{{ food.protein }}</td>
                        <td>{{ food.carbs }}</td>
                        <td>{{ food.fat }}</td>
                    </tr>
                    {% endfor %}
                </table>
            {% endfor %}
          {% else %}
              <a class="btn btn-primary btn-block" href="{{ path('mount_diet_days_day', {'date': "now"|date("Ymd")}) }}">Dodaj posiłki</a>
          {% endif %}
        </div>
      </div>
    </div>

    <div class="panel panel-default">
      <div class="panel-heading">
        <h4 class="panel-title">
          <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion" href="#collapseSupper">Kolacja</a>
        </h4>
      </div>
      <div id="collapseSupper" class="panel-collapse collapse">
        <div class="panel-body">
          {% if day.getSupper().count() %}
            {% for supper in day.getSupper() %}
                <h3>{{ supper.getName() }}</h3>
                <div class="day-meal-header">
                    Kalorie: {{ supper.getCalories() }}
                    Białka: {{ supper.getProtein() }}
                    Węglowodany: {{ supper.getCarbs() }}
                    Tłuszcze: {{ supper.getFat() }}
                </div>

                <table>
                    <tr>
                        <th>Nazwa</th>
                        <th>Waga</th>
                        <th>Kalorie</th>
                        <th>Białka</th>
                        <th>Węglowodany</th>
                        <th>Tłuszcze</th>
                    <tr>

                    {% for food in supper.getFood() %}
                    <tr class="meal-food">
                        <td>{{ food.getFood.name }}</td>
                        <td>{{ food.serving }}</td>
                        <td>{{ food.calories }}</td>
                        <td>{{ food.protein }}</td>
                        <td>{{ food.carbs }}</td>
                        <td>{{ food.fat }}</td>
                    </tr>
                    {% endfor %}
                </table>
                {% endfor %}
          {% else %}
              <a class="btn btn-primary btn-block" href="{{ path('mount_diet_days_day', {'date': "now"|date("Ymd")}) }}">Dodaj posiłki</a>
          {% endif %}
        </div>
      </div>
    </div>
  </div>
{% endblock %}