<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="/css/diet.css">

        <title>{% block title %}Dieta Wspomagacz{% endblock %}</title>
    </head>
    <body>

        <nav class="navbar-wrapper navbar-inverse" role="navigation">
            <div class="container">
              <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Dieta Wspomagacz</a>
              </div>
              <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                  <li><a href="{{ path('mount_diet_days') }}">Kalendarz</a></li>
                  <li><a href="{{ path('mount_diet_meals') }}">Posiłki</a></li>
                  <li><a href="{{ path('mount_diet_food') }}">Jedzenie</a></li>
                </ul>
              </div>
            </div>
        </nav>

        <div class="container">

            {% if header is defined %}<header class="page-header"><h1>{% block header %}{% endblock %}</h1></header>{% endif %}

            <section id="messages">
                {% if app.session.hasFlash('notice') %}
                    <div class="notice">{{ app.session.flash('notice') }}</div>
                {% endif %}
            </section>
            <section id="content">
                {% block content %}{% endblock %}
            </section>
        </div>

        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
        <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.0/js/bootstrap.min.js"></script>
        <script src="/js/diet.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>